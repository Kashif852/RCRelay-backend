class Product {
    constructor(id, name, price) {
      this.id = id; // Unique identifier for the product, like 'ipd' for 'Super iPad'
      this.name = name; // The name of the product
      this.price = price; // The price of the product
    }
  }
  
  module.exports = Product;
  